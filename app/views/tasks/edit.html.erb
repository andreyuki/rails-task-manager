<div class="container">
  <h1 class="mt-3">Edit Task</h1>
  <%= render 'tasks/form' %>
  <div class="border-top mt-3 text-primary">
    <%= link_to task_path(@task), class: "text-decoration-none" do %>
      <i class="fa-solid fa-clock-rotate-left mt-3"></i>
      <span>Back to task</span>
    <% end %>
  </div>

</div>
